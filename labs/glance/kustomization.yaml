apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lab

resources:
  - deployment.yaml

configMapGenerator:
  - name: glance-config
    files:
      - config/glance.yml
      - config/home.yml
  - name: glance-assets
    files:
      - assets/user.css
